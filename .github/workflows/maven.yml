# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with Maven

on:
  push:
   
jobs:
# Job#1
  Build:

    runs-on: self-hosted
    env:
     JAVA_HOME: C:\Program Files\Java\jdk-11.0.10

    steps:
    
    - name: Checkout the code
      uses: actions/checkout@v2
      with:
       ref: main
      
    
    - name: Build with Maven
      run: |
           cd HelloWorld;
           mvn clean install
           
   # - name: Upload to Artifactory
     # run:  Remove-item alias:curl;
         #   curl -X PUT -u ${{ secrets.ARTIFACTORY_USER }}:${{ secrets.ARTIFACTORY_PASSWORD }} -T C:\GitHub_Actions\POC_DEMO\actions-runner-win-x64-2.281.0\Workspace_POC\POC_DEMO\POC_DEMO\HelloWorld\target\HelloWorld.war "http://localhost:8082/artifactory/CG-libs-release-local/POC/HelloWorld.war"
   
   # - name: Download Artifact from Artifactory
    #  run: Remove-item alias:curl;
      #     curl -o C:\GitHub_Actions\POC_DEMO\HelloWorld.war -u ${{ secrets.ARTIFACTORY_USER }}:${{ secrets.ARTIFACTORY_PASSWORD }} "http://localhost:8082/artifactory/CG-libs-release-local/POC/HelloWorld.war"
   
  
   

node {
  stage('SCM') {
    checkout scm
  }
  stage('SonarQube Analysis') {
    //def mvn = tool 'Default Maven';
    withSonarQubeEnv() {
     // sh "${mvn}/bin/mvn sonar:sonar"
     bat "mvn clean verify sonar:sonar '-Dsonar.login=b68f58b9b31d9630a655d563d1af1bce43fb7db8'
    }
  }
}

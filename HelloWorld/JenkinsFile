node {
  stage('SCM') {
    checkout scm
  }
  stage('SonarQube Analysis') {
    //def mvn = tool 'Default Maven';
    withSonarQubeEnv() {
     // sh "${mvn}/bin/mvnsonar:sonar"
     bat "mvn --file /HelloWorld/pom.xml sonar:sonar"
    }
  }
}
